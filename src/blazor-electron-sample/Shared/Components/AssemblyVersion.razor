@using System.Reflection
@using System.Diagnostics
@AssemblyVersionAsString

@code {
    private string AssemblyVersionAsString => FileVersionInfo.GetVersionInfo(GetType().Assembly.Location).ProductVersion;
}
